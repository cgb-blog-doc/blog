.album-wrap {
    display:flex;
    flex-wrap:wrap;
}

.album-wrap img{
    width:33.33%;
}

a {
    word-break:break-all;
}

.home .footer {
    padding:2.5rem 0 !important;
}

div[class*="language-"]::before {
    position: absolute;
    z-index: 3;
    top: 0.8em;
    right: 1em;
    font-size: 0.75rem;
    color: rgba(255,255,255,0.4);
    height: 16px;
    line-height: 16px;
}